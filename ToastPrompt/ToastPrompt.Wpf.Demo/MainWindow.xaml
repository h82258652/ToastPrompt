<Window x:Class="Controls.Wpf.Demo.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:controls="clr-namespace:Controls;assembly=ToastPrompt.Wpf"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:fa="http://schemas.fontawesome.io/icons/"
        Title="MainWindow"
        Width="525"
        Height="350"
        mc:Ignorable="d">
    <Grid x:Name="RootGrid">
        <Viewbox      Width="50"
                      Height="50"
                      HorizontalAlignment="Center"
                      VerticalAlignment="Center">
            <fa:FontAwesome Icon="InfoCircle"
                            Foreground="Blue"></fa:FontAwesome>
        </Viewbox>
        <StackPanel HorizontalAlignment="Right"
                    VerticalAlignment="Bottom"
                    Orientation="Vertical">
            <Button Content="ShowMessage"
                    Click="MessageClick"></Button>
            <Button Content="ShowError"
                    Click="ErrorClick"></Button>
            <Button Content="ShowInfo"
                    Click="InfoClick"></Button>
            <Button Content="ShowWarning"
                    Click="WarningClick"></Button>
        </StackPanel>
    </Grid>
</Window>